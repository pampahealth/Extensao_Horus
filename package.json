{
  "name": "teste-plugin-",
  "version": "1.1.0",
  "description": "",
  "main": "background.js",
  "scripts": {
    "build": "esbuild src/popup.js --bundle --outfile=dist/popup.js --format=esm && esbuild background.js --bundle --outfile=dist/background.js --format=esm"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@supabase/supabase-js": "^2.89.0"
  },
  "devDependencies": {
    "esbuild": "^0.20.2"
  }
}
